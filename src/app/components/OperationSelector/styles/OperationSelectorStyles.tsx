import styled from 'styled-components';
import { Form } from 'react-bootstrap';

export const OperationSelectContainer = styled(Form)`
  margin-bottom: 1.5rem;
`;

export const OperationSelect = styled(Form.Select)`
  max-width: 300px;
  margin: 0 auto;
`;
