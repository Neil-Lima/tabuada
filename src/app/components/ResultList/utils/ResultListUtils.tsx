// Funções utilitárias para o componente ResultList
// Por enquanto, não há funções específicas necessárias, mas poderia incluir
// formatação de resultados, filtragem, etc.

export const formatResult = (result: string): string => {
    return result; // Sem formatação adicional por enquanto
  };
  